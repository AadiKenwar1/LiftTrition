// Dev-only V4 Login — identical to V3 (the brand opener is intentionally neutral: no color competes with
// the logo). Re-exported so the V4 flow stays self-contained without drift.
export { default } from './V3'
